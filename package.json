{
  "name": "BRU Tech Computer",
  "version": "beta",
  "scripts": {
    "build": "pug templates -o deploy && npm run copy:assets",
    "copy:assets": "mkdir -p deploy/assets && cp -r assets/ deploy/"
  },
  "dependencies": {
    "pug": "^3.0.2",
    "pug-cli": "^1.0.0-alpha6"
  }
}
